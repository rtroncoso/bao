{
  "name": "@bao/assets",
  "version": "0.1.0",
  "description": "Bao static game assets and local dev server",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rtroncoso/bao.git",
    "directory": "packages/assets"
  },
  "license": "ISC",
  "scripts": {
    "dev": "serve public -l 8787 --cors",
    "staging": "pnpm --filter @bao/cli exec bao deploy --environment staging",
    "production": "pnpm --filter @bao/cli exec bao deploy --environment production"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
