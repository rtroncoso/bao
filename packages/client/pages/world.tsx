import { WorldRoomContainer } from '@/components';
import { NextPage } from 'next';
import React from 'react';

export const GamePage: NextPage = (props) => <WorldRoomContainer {...props} />;
export default GamePage;
