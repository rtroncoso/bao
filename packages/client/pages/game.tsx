import { GameContainer } from '@/components';
import { NextPage } from 'next';
import React from 'react';

export const GamePage: NextPage = (props) => <GameContainer {...props} />;
export default GamePage;
