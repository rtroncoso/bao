# Environment variables live in the repo root.
# Copy /.env.example to /.env and configure there.
#
# Client URLs use NEXT_PUBLIC_* so they work locally and on Vercel:
#   NEXT_PUBLIC_BAO_API, NEXT_PUBLIC_BAO_SERVER, NEXT_PUBLIC_BAO_ASSETS
